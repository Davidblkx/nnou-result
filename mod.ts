/**
 * @module Result
 *
 * @example
 * ```ts
 * import { Result, ok, err } from '@nnou/result';
 *
 * const result = ok(42); // Returns Result<number, never>
 * ```
 */

export * from './assert.ts';
export * from './result.ts';
export * from './resolve.ts';
